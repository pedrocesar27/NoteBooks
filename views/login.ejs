<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/login.css">
    <title>Login NoteBooks</title>
</head>
<body>
    <div class="container">
        <div class="box-login">
            <div class="login-logo">
                <img class="icon" src="/assets/icons/journal-bookmark-fill" alt="logo">
                <h1>NoteBooks</h1>
            </div>
            <% if(error){ %>
                <div class="box-hidden">
                    <h3>Something went wrong!</h3>
                    <p><%= error %></p>
                </div>
            <% } %>
            <form class="form-login" action="/login" method="POST">
                
                <input class="input-logo" type="email" name="email" id="email" placeholder="Email Address">
                <input class="input-logo" type="password" name="password" id="password" placeholder="Password">

                <button type="submit">Login</button>
                <p>New here? <a href="/createAccount">Sign up now!</a></p>
            </form>
        </div>
    </div>
</body>
</html>